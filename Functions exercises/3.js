// Write a function that checks if a given number is a three digit long number.

function threeDigitNumber(a){
    if(a > 99 && a < 1000){
        console.log('Number is three digit long!')
    }
}

threeDigitNumber(990)